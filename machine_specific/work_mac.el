;;; machine_specific/work_mac.el -*- lexical-binding: t; -*-
(set-frame-width (selected-frame) 300)
(set-frame-height (selected-frame) 100)

(setq org-file-path "~/Org/")

(setq doom-font (font-spec :family "JuliaMono" :size 18)
      doom-variable-pitch-font (font-spec :family "Helvetica" :size 18))

(setq org-journal-file-type 'yearly)
